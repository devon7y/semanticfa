options(error = function(e) {traceback(); quit(status = 1)})
suppressPackageStartupMessages({library(semanticfa); library(data.table)})
cat("libs ok\n")
load("data/big5.rda")
X <- t(big5$embeddings); R <- cor(X); N <- nrow(X)
cat("data ok", dim(X), "\n")
map <- psych::VSS(R, n = 12, n.obs = N, plot = FALSE, fm = "minres")
cat("MAP k =", which.min(map$map), "\n")
ekc <- EFAtools::EKC(R, N = N)
cat("EKC k =", ekc$n_factors, "\n")
