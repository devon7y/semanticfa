% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naming.R
\name{sfa_name}
\alias{sfa_name}
\title{Name the Factors of an sfa Fit}
\usage{
sfa_name(
  fit,
  model = NULL,
  pool = NULL,
  n_candidates = 5L,
  instruction = NULL,
  collision = TRUE,
  loo_sets = TRUE,
  salient = NULL,
  block_size = 50000L,
  ...
)
}
\arguments{
\item{fit}{An object of class \code{sfa}.}

\item{model}{Embedding model used for naming. Default \code{NULL} reuses
the model the fit was embedded with. Passing a different model (e.g. a
larger one) switches naming to that model while keeping the fitted
factor structure - extraction and naming reward different model
properties, and a larger namer typically improves label abstraction.}

\item{pool}{An \code{sfa_pool} object, or \code{NULL} to fetch/build the
pool for \code{model} via \code{\link[=sfa_pool]{sfa_pool()}}.}

\item{n_candidates}{Length of the gated candidate list per factor
(default 5).}

\item{instruction}{Override the naming instruction (see
\code{\link[=sfa_naming_instruction]{sfa_naming_instruction()}}); a warning notes that results were
validated under the default.}

\item{collision}{Resolve duplicate labels across factors via the
geometric keeper (default \code{TRUE}).}

\item{loo_sets}{Compute leave-one-out candidate sets (default
\code{TRUE}).}

\item{salient}{Loading threshold defining a factor's items for naming:
items whose primary factor this is. Reserved for future use; the
assignment rule is primary-loading, as in the research method.}

\item{block_size}{Pool rows per block during retrieval (memory knob).}

\item{...}{Passed to the embedding backend.}
}
\value{
An object of class \code{sfa_labels}: a data.frame with one row
per factor (\code{factor}, \code{label}, \code{rule},
\code{candidates}, \code{n_items}, \code{collision_moved}) plus
attributes \code{gated}, \code{model}, \code{instruction}.
}
\description{
Retrieves a verbal label for every factor of an \code{\link[=sfa]{sfa()}} fit by embedding
the fit's items under a construct-retrieval instruction, building one
naming target per factor, and ranking a large pre-filtered candidate
pool. Deterministic: the same fit, model, and pool always produce the
same labels.
}
