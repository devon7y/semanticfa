% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{sfa_deletion_gaps}
\alias{sfa_deletion_gaps}
\title{Newly Uncovered Content After an Item-Set Change}
\usage{
sfa_deletion_gaps(reference, reduced, quotes = 3L, top = 8L)
}
\arguments{
\item{reference}{The reference audit (\code{"sfa_coverage"}), typically the
full scale.}

\item{reduced}{The changed item set's audit, run against the same
region and anchored to \code{reference$radius}.}

\item{quotes}{Number of example sentences to return. Default 3, ordered
by how far the change strands them (largest increase in
nearest-item distance first).}

\item{top}{Number of label terms. Default 8.}
}
\value{
A list: \code{n_new} (newly uncovered sentences), \code{share_new}
(their fraction of the region), \code{coverage_drop}
(\code{reference$coverage - reduced$coverage}), \code{terms} (distinctive terms
of the newly uncovered content against the still-covered content),
and \code{quotes}.
}
\description{
Compares two audits of the \emph{same region} - a reference audit and an
audit of a changed item set (items deleted, a short form, a revision) -
and reports the content the change strands: the region sentences covered
by the reference scale but not by the changed one. Labeling only this
\emph{delta} is what makes a deletion study readable. The changed scale's
full gap report mixes newly created gaps with whatever the reference
scale already failed to cover, and the pre-existing uncovered mass
usually drowns the signal.
}
\details{
Both audits must be run against the same region with the same filters,
and the changed audit must be \strong{anchored} to the reference audit's
radius (\code{radius = reference$radius}) - otherwise recalibration moves
the yardstick along with the item set and the delta confounds the two.
The function checks both conditions.
}
