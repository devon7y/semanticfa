% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regions_multi.R
\name{sfa_reembed_region}
\alias{sfa_reembed_region}
\title{Re-Embed a Construct Region Under Another Encoder}
\usage{
sfa_reembed_region(
  region,
  embed = "sbert",
  model = NULL,
  cache = TRUE,
  file = NULL
)
}
\arguments{
\item{region}{An \code{"sfa_region"} (or path to one).}

\item{embed, model, cache}{As in \code{\link[=sfa_build_region]{sfa_build_region()}}.}

\item{file}{Optional path to save the re-embedded region.}
}
\value{
The \code{"sfa_region"} with new \code{embeddings} and \code{encoder}.
}
\description{
Takes a region's sentences (from \code{\link[=sfa_build_region]{sfa_build_region()}} or
\code{\link[=sfa_build_regions]{sfa_build_regions()}}, including sentence-only regions built with
\code{embeddings = FALSE}) and embeds them under the given encoder, returning
a region in that encoder's space. This is how an encoder-ladder study
reuses one extraction: the regions differ only in embedding space, never
in text.
}
