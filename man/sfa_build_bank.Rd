% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{sfa_build_bank}
\alias{sfa_build_bank}
\title{Pre-Embed a Set of Texts into a Reusable Bank}
\usage{
sfa_build_bank(
  texts,
  instruction = TRUE,
  embed = "sbert",
  model = NULL,
  cache = TRUE,
  file = NULL
)
}
\arguments{
\item{texts}{Character vector of raw texts (items, definitions). Wrapped
with \code{instruction} exactly as \code{\link[=sfa_coverage]{sfa_coverage()}} wraps them, so lookups
match.}

\item{instruction}{\code{TRUE} (the construct-retrieval instruction the
regions use, default), \code{FALSE}, or a custom string. Must match the
regions the bank will be used with.}

\item{embed, model, cache}{As in \code{\link[=sfa_embed]{sfa_embed()}}.}

\item{file}{Optional path to save the bank (\code{.rds}).}
}
\value{
An \code{"sfa_bank"}: vectors, texts, instruction, encoder,
timestamp.
}
\description{
Embeds texts (typically: every scale item, every construct definition,
and every planned narrowing of a study) under one encoder and stores the
vectors keyed by the exact strings \code{\link[=sfa_coverage]{sfa_coverage()}} will pass to its
embedder. Feed the result to audits via \code{\link[=sfa_embedding_bank]{sfa_embedding_bank()}}.
}
