% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regions_multi.R
\name{sfa_build_regions}
\alias{sfa_build_regions}
\title{Build Many Construct Regions in One Corpus Pass}
\usage{
sfa_build_regions(
  constructs,
  corpus = "fineweb-10bt",
  target = 1500,
  max_docs = 2e+07,
  sentences_per_doc = 3,
  min_chars = 30,
  max_chars = 500,
  embeddings = TRUE,
  embed = "sbert",
  model = NULL,
  cache = TRUE,
  instruction = TRUE,
  dir = NULL,
  progress = TRUE
)
}
\arguments{
\item{constructs}{Named list: construct name -> \code{list(definition = , variants = NULL)} (a bare definition string also works). Default
variants are simple inflections of the name.}

\item{corpus}{\code{"fineweb-10bt"} (streamed once for all constructs), or a
local corpus as in \code{\link[=sfa_build_region]{sfa_build_region()}}.}

\item{target, max_docs, sentences_per_doc, min_chars, max_chars}{As in
\code{\link[=sfa_build_region]{sfa_build_region()}}, applied per construct.}

\item{embeddings}{Embed each region now? \code{FALSE} builds sentence-only
regions (no encoder needed; \code{\link[=sfa_coverage]{sfa_coverage()}} refuses them until
\code{\link[=sfa_reembed_region]{sfa_reembed_region()}} fills the embeddings in).}

\item{embed, model, cache, instruction}{As in \code{\link[=sfa_build_region]{sfa_build_region()}}.}

\item{dir}{Optional directory: each region is saved as
\verb{\{dir\}/\{slug\}.rds}.}

\item{progress}{Print streaming progress? Default \code{TRUE}.}
}
\value{
A named list of \code{"sfa_region"} objects.
}
\description{
The campaign-scale companion to \code{\link[=sfa_build_region]{sfa_build_region()}}: streams the corpus
once and extracts sentences for every construct simultaneously, then
builds one \code{"sfa_region"} per construct. Variants are matched on \strong{word
boundaries} (a component like "care" must not match "career"), which
also makes compositional names safe: a construct like Honesty-Humility
is gathered through \code{variants = c("honesty", "humility")}.
}
\details{
For encoder-ladder studies, build once with \code{embeddings = FALSE} (a pure
extraction; no encoder touched) and embed the same sentence sets under
each encoder with \code{\link[=sfa_reembed_region]{sfa_reembed_region()}} - the regions differ only in
the embedding space, never in their text.
}
