% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{sfa_embedding_bank}
\alias{sfa_embedding_bank}
\title{Use a Pre-Generated Embedding Bank as an Embedder}
\usage{
sfa_embedding_bank(bank)
}
\arguments{
\item{bank}{An \code{"sfa_bank"} from \code{\link[=sfa_build_bank]{sfa_build_bank()}}, or a path to one.}
}
\value{
A function \code{(texts) -> matrix}, for use as \verb{embed = }.
}
\description{
Turns an \code{"sfa_bank"} (or a path to one) into a lookup function that
\code{\link[=sfa_coverage]{sfa_coverage()}} and \code{\link[=sfa_embed]{sfa_embed()}} accept as their \code{embed} argument, so
audits run entirely from published embeddings - no encoder loads.
}
\details{
A lookup miss is an error, not a fallback: the bank must contain every
item, definition, and planned narrowing the audit touches. The error
lists what is missing so the bank can be rebuilt to include it.
}
\examples{
\dontrun{
bank <- sfa_embedding_bank("bank_qwen8b.rds")
region <- sfa_load_region("regions_qwen8b/procrastination.rds")
audit <- sfa_coverage(my_items, region, embed = bank,
                      model = region$encoder)
}
}
