% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{sfa_build_region}
\alias{sfa_build_region}
\title{Build a Construct Region from a Text Corpus}
\usage{
sfa_build_region(
  construct,
  definition,
  corpus = "fineweb-10bt",
  target = 1500,
  max_docs = 2e+07,
  sentences_per_doc = 3,
  min_chars = 30,
  max_chars = 500,
  variants = NULL,
  embed = "sbert",
  model = NULL,
  instruction = TRUE,
  cache = TRUE,
  file = NULL,
  progress = TRUE
)
}
\arguments{
\item{construct}{Construct term to search for, e.g. \code{"procrastination"}.}

\item{definition}{One- or two-sentence definition of the intended sense.
Stored with the region and used as the default sense-gate seed in
\code{\link[=sfa_coverage]{sfa_coverage()}}.}

\item{corpus}{\code{"fineweb-10bt"} (default; streams from the Hugging Face
Hub), a character vector of documents, a data frame with a
\code{text}/\code{item} column, or a character vector of file or directory paths.}

\item{target}{Stop once this many matching sentences are collected.
Default 1500.}

\item{max_docs}{Maximum number of corpus documents to scan. Default
\code{2e7} (covers the full FineWeb 10BT sample). Lower it to cap runtime on
a laptop; the saturation diagnostics in \code{\link[=sfa_coverage]{sfa_coverage()}} show whether
the smaller region was enough.}

\item{sentences_per_doc}{Maximum sentences kept per document (guards
against one document flooding the region). Default 3.}

\item{min_chars, max_chars}{Sentence length bounds. Defaults 30 and 500.}

\item{variants}{Character vector of term spellings to match. Default
\code{NULL} generates simple morphological variants of \code{construct}.}

\item{embed, model, cache}{Passed to \code{\link[=sfa_embed]{sfa_embed()}}; \code{model} defaults to the
package's default encoder. The audit must use the same encoder, which
\code{\link[=sfa_coverage]{sfa_coverage()}} enforces from the region's metadata.}

\item{instruction}{\code{TRUE} (default) embeds sentences under the
construct-retrieval instruction (recommended: this register alignment
outperformed alternatives in validation), \code{FALSE} embeds raw text, or a
custom instruction string.}

\item{file}{Optional path; when given, the region is saved there with
\code{\link[=saveRDS]{saveRDS()}} and can be reloaded with \code{\link[=sfa_load_region]{sfa_load_region()}}.}

\item{progress}{Print progress while streaming. Default \code{TRUE}.}
}
\value{
An object of class \code{"sfa_region"}: a list with the sentences and
their sources, the embedding matrix, and full provenance metadata.
}
\description{
Assembles the corpus half of a content-validity audit: a \emph{construct
region}, the cloud of real sentences that mention a construct term,
embedded in the same space that \code{\link[=sfa_coverage]{sfa_coverage()}} will embed the scale's
items into. The result is a self-contained object recording its own
provenance (corpus, extraction parameters, encoder, date); save it with
\verb{file =} and archive it with your analysis so the audit is reproducible.
}
\details{
Two kinds of corpus are supported. \code{corpus = "fineweb-10bt"} streams the
\verb{sample-10BT} configuration of the FineWeb corpus (a documented random
sample of a modern LLM-training corpus) from the Hugging Face Hub via the
Python \code{datasets} package, stopping as soon as \code{target} sentences are
found or \code{max_docs} documents have been scanned; common construct terms
hit their quota within minutes, rare terms scan the full sample (use an
HPC batch job, or lower \code{max_docs} and check saturation). Alternatively,
pass your own corpus: a character vector of documents, a data frame with
a \code{text} column, or paths to plain-text files or directories. A
domain-specific corpus (for example, workplace communications for a
workplace construct) is a fully disclosed design choice recorded in the
region's provenance.

Sentences are \emph{not} sense-filtered here: the region stores every mention,
and \code{\link[=sfa_coverage]{sfa_coverage()}} applies its sense gate at audit time against the
definition supplied there. This is what makes construct narrowing cheap:
one region file for "procrastination" can be re-audited as "academic
procrastination" by re-gating with a narrower definition, with no new
extraction.
}
\examples{
\dontrun{
region <- sfa_build_region(
  construct  = "procrastination",
  definition = paste("Procrastination is the voluntary delay of an",
                     "intended action despite expecting to be worse off."),
  file       = "procrastination_region.rds"
)

# a laptop-friendly build capped at 2M documents
region <- sfa_build_region("procrastination", definition = "...",
                           max_docs = 2e6)

# your own corpus
region <- sfa_build_region("procrastination", definition = "...",
                           corpus = "~/corpora/workplace_emails/")
}
}
\seealso{
\code{\link[=sfa_coverage]{sfa_coverage()}} to audit a scale against the region,
\code{\link[=sfa_load_region]{sfa_load_region()}} to reload a saved region.
}
