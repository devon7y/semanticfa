% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{sfa_region_bank}
\alias{sfa_region_bank}
\title{Use a Region's Own Stored Embeddings as an Embedder}
\usage{
sfa_region_bank(region)
}
\arguments{
\item{region}{An \code{"sfa_region"} (or path to one) with embeddings.}
}
\value{
A function \code{(texts) -> matrix}, for use as \verb{embed = }.
}
\description{
Turns an \code{"sfa_region"} into a lookup function over its own sentences,
for use as \verb{embed =} when the "items" of an audit are drawn from a
region itself. The canonical use is the cross-audit region-overlap
baseline: sentences sampled from construct region A are audited as
pretend items against region B, so their embeddings come from A's own
stored matrix and no encoder loads. Requires the two regions to share
the same instruction (regions built together always do).
}
