% discussion_future.tex
\subsection{Future Directions}

The most direct extension is comparative: coherence estimated across
instruments, domains, and populations until it acquires norms. The
demonstration---deliberately well-built, its coherence
domain-graded---is one data point; the interesting variation lies between
construct types: the symptom-network versus common-cause ordering
derived above (\citealp{borsboom-2008}), construct-dependence in
multilevel analyses \citep{zhang-qiu-2026}, and the lexical-versus-behavioral
phrasing gradient \citep{milano-etal-2025b} await systematic test with a
common toolkit. A second is methodological hardening: encoder
robustness reported across embedding models as reliability is across
raters \citep{mandera-etal-2017}; embedding-depth selection promoted from
diagnostic to tuning step, psychometric signal concentrating in coordinate
subsets \citep{golino-2026}; redundancy cutoffs recalibrated on semantic
similarity matrices, not inherited from response-data simulation
\citep{christensen-etal-2023}; and Monte Carlo calibration grown into
community null baselines indexed by item count, embedding dimension, and
encoding, and matched on the observed general similarity component, so
calibrated reporting \citep{pokropek-2026} costs a lookup,
not a simulation. The retention null deserves to be singled out within
that program, because no adequate replacement exists yet. The isotropic
null reduces the adapted parallel analysis to a slightly elevated
latent-root criterion, and the natural candidates fail in complementary
ways on embedding matrices: permutation-style resampling overextracts
\citep{garrido-etal-2025}, and model-based comparison data saturate
because factor models with diagonal uniqueness cannot reproduce the
anisotropic spectral tail (Supplementary Materials;
\citealp{ruscio-roche-2012}). A usable retention test for this data
class needs reference populations that are generative over item
embeddings themselves, with known structure planted in a realistic
embedding geometry, a benchmark a machine-learning extension in the
spirit of the comparison data forest could then be trained against
\citep{goretzko-ruscio-2024}. Until that exists, retention on embedding
similarity matrices is best reported as the consensus of complementary
criteria alongside the misfit profile, with congruence tracked across
candidate counts.

A third extends the pre-data use case across languages: structured
generative adaptation performs at or near human benchmarks
\citep{grobelny-etal-2025} but lacks a screen; the package suggests
one---source and adapted items embedded in a shared multilingual
space, congruence metrics repurposed as a pre-fielding check for
structural drift in translation. All three keep the division of labor
these results recommend. More than thirty years ago,
\citet{lara-etal-1992} predicted that semantic interpretation of factor
solutions would have to be mechanized; embedding geometry has made it
computable, and \texttt{semanticfa} makes it routine. What remains
human---on this evidence, necessarily---is what wording cannot
carry: response processes, criterion relations, and the judgment that
turns a characterized proposal into a validated measure.
