# Validation Report — semanticfa: An R Package for Response-Free Semantic Factor Analysis of Psychometric Scales

Manuscript state: working tree, 2026-07-11 (post BRM-compliance revision, abstract rewrite, and full em-dash/semicolon purge + "exploratory" reframing: abstract 245 words plain-language, Declarations + Open Practices Statement before References, jackknife CIs on headline r and mean matched phi, golino-etal-2021 primary TEFI cite; 74 pp compiled). NOTE: abstract- and prose-touching claim-pass units are STALE against the rewrite + purge, reference pass unaffected (no citation keys or claim meaning changed) - spot-check claim consistency on the next round.
Overall status: YELLOW (mechanical pre-flight green incl. punctuation now ENFORCED; reference validation round 2 done, 0 SERIOUS, 4/5 MINOR fixed; claim pass round 1 done, all findings fixed; OSF link + CRAN 0.1.2 alignment still open)

## Em-dash / semicolon purge + "exploratory" reframing — 2026-07-11

Author directive: enforce the write-paper absolute ban (no em dashes `---`, no semicolons `;`) that this manuscript had been exempting by authorial style, and drop "exploratory" as broad framing while KEEPING it where the demonstration genuinely runs an exploratory analysis. Executed as a Fable-5 fan-out (`dev/scripts/punctuation-purge.workflow.js`, one agent per file, 29 files) under strict constraints: verbatim/macros/numbers/citations/"exploratory" untouched. Result: 114 prose em dashes + 137 prose semicolons -> 0/0. Verified: (a) transcript verbatim blocks BYTE-IDENTICAL to committed baseline (md5, 7 transcript files, 0 changed - transcript fidelity preserved); (b) 0 \val macros dropped or swapped (git-diff net-usage check) and used-subset-of-defined holds; (c) compile clean, 0 undefined, 0 "??", clean blg; (d) abstract 245 words. Also: generated encoding-table NA marker changed from em dash to en dash in `make_paper_assets.R` (`enc_cell`), regenerated. Manuscript.tex residual `;`/em dash are in LaTeX COMMENTS only (never rendered). "exploratory" now appears in exactly 3 legitimate spots: "Exploratory Graph Analysis (EGA)" (method name, introduction_pipelines), "exploratory-graph factors" (EGA, introduction_taxonomy), and "a conventional exploratory factor analysis (EFA)" of the human benchmark (methods_materials, the demonstration's actual exploratory analysis). Four broad-framing uses ("SFA: exploratory factor analysis of...", "the full discipline of exploratory factor analysis", etc.) changed to plain "factor analysis" in introduction_overview/introduction_package/discussion_summary/conclusion_summary.

## BRM-compliance revision + validation round — 2026-07-09/10

Journal audit (Behavior Research Methods) fixes applied: abstract trimmed 261→249 (cap 250, thin margin — recount after ANY abstract edit); `sections/declarations.tex` (Funding/COI/Ethics/Data/Code) and `sections/open_practices.tex` (moved from Method, now immediately before References, present-tense OSF wording + not-preregistered sentence) added; author note now has funding/COI/postal address/telephone; `methods_llm_use.tex` documents LLM assistance; `reproduce/make_ci_assets.R` (delete-one-item jackknife, 50 replicates, both solutions refit + re-matched per deletion, Fisher-z for r) feeds `output/ci_jackknife.csv` → 4 new macros: r = .46 [.33, .56], mean matched phi = .744 [.610, .878]; method sentences added to Analysis Plan. reproduce.R/reproduce.Rout untouched (transcript byte-fidelity preserved); make_paper_assets.R asserts CI CSV freshness against stored point estimates.

**Reference validation ROUND 2 (2026-07-10): 82 CORRECT / 5 MINOR / 0 SERIOUS (87 keys, incl. 15 first-round).** Fixes applied: larsen-bong-2016 "outperformed doctoral-student judges" hedged to competitive/task-dependent (introduction_taxonomy); reimers-gurevych-2019 bib +pages+DOI; suarez-alvarez-etal-2026 4th author initial E. D.→D.; yanitski-westbury-2026 bib note 0.1.0→0.1.2 (matches prose). LEFT FOR AUTHOR: golino-2026 best-source flag — TEFI-as-fit-index sites would be strengthened by the primary TEFI paper (Golino et al. 2021, MBR 56(6):874-902; PDF not in library — propose/download before citing); reimers "first enabled cosine-comparable embeddings" mild precedence flag (defensible, author's call). Convergence NOT yet declared: a confirming round 3 (or author endorsement of the residue) is the stopping rule.

**Claim pass ROUND 1 (2026-07-10): 61 CORRECT / 6 MINOR / 3 SERIOUS across 70 units (5 units' verifiers hit schema-retry failures; re-run in progress).** All 9 findings fixed: (S) "every exported function" overclaim (5 of 27 exports never run: naming trio, tsneplot alias, install helper) → "all five workflow stages"/"full five-stage workflow" in abstract + introduction_package + discussion_summary; (S) "four-method consensus" stale after EKC update → "five-criterion" in methods_analysis, EKC vote added to both Discussion parentheticals; (S) "nine demonstration stages" → ten (matches the ten \emph stage paragraphs); (M) "aligning worst" → "worst among cosines" in abstract + discussion_summary (NLI keyed is lower); (M) "within-domain" dropped from the eleven-pairs claim (only the displayed ten verified within-domain); (M) "settles negatively" → "provides evidence against" (discussion_encodings); (M) "any scale's structure" → "a scale's structure" (conclusion); (M) "every number checkable against a transcript" → "nearly every ... jackknife intervals excepted" (discussion_use_cases); (M) "respondent-free" → "response-free" (conclusion, terminology).

## Transcript regeneration — 2026-07-03 (second: semanticfa 0.1.2, local)

`reproduce.Rout` regenerated under semanticfa 0.1.2 on the author's Apple-silicon macOS machine (arm64, R 4.5.0, seed 42), via a launchd Interactive agent (detached/background launches are QoS-throttled ~10x on this machine). Motivation: 0.1.2 adds the empirical Kaiser criterion to the `sfa_nfactors()` default consensus (new EKC row in Sections 5 and 9b: six at every model size; consensuses unchanged at six/six/five), Velicer's MAP (available, non-default), and the `sfa_cd()` comparison-data misfit profile, plus a new transcript Section 10b (CD profiles for the three model sizes and the human benchmark, human subsample crossover, `figures/fig_cd_profiles.pdf`). All previously reported values are unchanged except: psych's arbitrary MR factor names rotated again (N=MR3 stable; C=MR1, A=MR2, E=MR5, O=MR4 — absorbed by the `\valMr*` macros and byte-patched in the four excerpt blocks), local timings (NLI 70 s, embed 114.3 s, dimselect 28 s, calibrate 123.9 s), and the live-re-embed minimum cosine back at .9999 (was .9998 on the cluster BLAS). values.tex now carries 126 macros; 10 figures staged (new: `cd_profiles.pdf`). Verbatim-excerpt integrity re-verified programmatically: 267 excerpt lines, 4 remaining deviations, all the long-standing system.time de-wrapping convention (`t_x <- system.time(expr)` excerpted as `> expr`), content identical.

New supplementary subsection (Comparison-Data Misfit Profiles): human elbow at exactly k=5 (57.2% improvement then 19.7%), embedding profiles smooth (9–22% per step, 47–49% of baseline misfit left at k=5), sequential rule saturates at the ten-factor cap for all embedding matrices and for human subsamples of 500+, terminating (seven) only at n=250. Emitter hard-asserts the saturation and EKC-constancy claims so a future run that breaks the prose fails the build.

## Transcript regeneration — 2026-07-03 (first: semanticfa 0.1.1, Rorqual)

`reproduce.Rout` regenerated under the released semanticfa 0.1.1 on a Rorqual (Alliance Canada) login node, R 4.5.0, seed 42, BATCH_EXIT:0. Every headline value is byte-identical to the June run (encoding table, loadings, omegas, Tucker matrix, congruence) with three classes of change, all synced into the manuscript: (1) psych's arbitrary MR factor-name assignment rotated (N=MR3 unchanged; C=MR2, A=MR5, E=MR4, O=MR1) — prose now uses emitter-derived `\valMr*` macros so labels can never go stale again; (2) the sequential parallel-analysis rule corrects 0.6B retention from 4 to 3 (4B stays 4, 8B stays 5; modal consensuses unchanged at six/six/five); (3) timings reflect the CPU-only cluster (NLI 4103.2 s, embed 128.2 s, dimselect 1.9 s, calibrate 59.5 s) and the live-re-embed minimum cosine drifted .9999 → .9998. Verbatim-excerpt integrity verified programmatically: every excerpt line matches the new Rout byte-for-byte except the manuscript's long-standing reflow of overlong lines (29 lines, all pre-dating this revision and content-identical) and deliberate elision markers.

Pass status:
- Mechanical pre-flight : PASS (2026-07-10, this revision: compile clean, 0 undefined, blg clean, 0 ??, abstract 249/250, end-matter order Declarations→OPS→References verified, val used ⊆ defined)
- Reference validation  : round 2 done (2026-07-10, 82 CORRECT / 5 MINOR / 0 SERIOUS; 4 minors fixed, 1 author-choice residue) — convergence pending a confirming round 3 or author endorsement
- Missing-citation pass : not run (one known gap remains scheduled: golino-2021 primary TEFI source, PDF pending author download)
- Statistics pass       : not run
- Claim pass            : round 1 done (2026-07-10, all 75 units now covered: 61 CORRECT / 6 MINOR / 3 SERIOUS in the main run, all 9 fixed; the 5 initially failed units resolved on re-run — a3/a22/c9 CORRECT, a19 MINOR fixed (SQuID unbundled from the keying-free upshot, unfactorability noted, discussion_encodings), c20 verified manually: all dimselect numbers flow from the six valDepth*/valTimeDimselect macros, transcript literal matches)

## Mechanical pre-flight — 2026-07-02

- Compile + cross-refs: PASS (latexmk clean; 0 undefined references/citations; 0 "??" in PDF)
- BibTeX log: PASS (no warnings/errors beyond the cosmetic others/6th-ed note)
- Left-over flags: PASS (0 MISSINGCITE/INCOMPLETE/TODO) — the deliberate OSF placeholder in `methods_open_practices.tex` remains and must be filled before submission
- Figures: PASS — all 9 figures staged by `reproduce/make_paper_assets.R` from `reproduce/figures/` (md5-verified; this fixed the stale mislabeled `loadings.pdf` whose MR1/MR4 column labels were swapped)
- Numbers discipline: PASS — 102 macros in `generated/values.tex`, every used `\val…` defined; 4 defined-but-unused spares (`\valConsensusK`, `\valEgaKWord`, `\valMcpRKeyed`, `\valTimeNli`) kept intentionally. Tables tab:tucker, tab:encodings, S1, S2 are generated from `reproduce/output/*.csv`. Remaining literals in prose are design constants (seed 42, thresholds .85/.25/0.20, item counts, model dims) and citation-derived values from sourced papers.
- Statistical sanity: N/A in the classical sense (no t/F/p triples); all values traced to `reproduce/output/` by construction of the macro pipeline
- Prohibited punctuation: NOW ENFORCED (2026-07-11 purge, see the dedicated section above) - 0 em dashes, 0 semicolons in prose. (Was NOT ENFORCED before that date: the manuscript's established style had used them throughout by authorial choice.)
- Abbreviations / initials: unchanged from the previously validated draft; no new abbreviations introduced by this revision

## Refresh loop

`reproduce/reproduce.R` (analysis) → `reproduce/make_paper_assets.R` (macros + tables + figures into `latex/generated/` and `latex/figures/`) → `latexmk -pdf -jobname=<slug> manuscript.tex`. The manuscript is a view over `reproduce/output/`; no data-derived number is hand-typed.

## Reference validation

| Round | CORRECT | MINOR | SERIOUS |
|------:|--------:|------:|--------:|
| 1 | 66 | 9 (fixes applied) | 0 |

Rounds 2–3 pending author go-ahead (`dev/scripts/validate-round2.workflow.js`). This revision changed citation-adjacent text in: methods_pipeline (Kaiser 1960 now cited for the latent-root rule; TMFG/EBICglasso wording), introduction_pipelines (ARI scoped to Wang et al.; t-SNE cite removed there, still cited in the same subsection's final paragraph), discussion_limitations (new cites of openpsychometrics-2018, qwen-2025, pokropek-2026, garrido-etal-2025, guenole-etal-2025 in the added limitation paragraphs) — include these in the next round.

THREE MORE REFERENCES added 2026-07-03 (retention update; PDFs in papers/, txt in papers_txt/, bib entries built from the PDFs): braeken-vanassen-2017 (EKC source, methods_pipeline + results_retention), ruscio-roche-2012 and goretzko-ruscio-2024 (comparison data / CDF, methods_pipeline + discussion_limitations + discussion_future + supplementary_retention). PLUS TWO MORE added later the same day once the author supplied the PDFs: velicer-1976 (MAP sentence, methods_pipeline) and ruscio-kaczetow-2008 (GenData generator sentence, supplementary_retention). All five are FIRST-ROUND candidates for the next reference-validation pass.

SEVEN NEW REFERENCES added 2026-07-02 (semantic theory of survey response literature; PDFs in papers/, txt in papers_txt/, bib entries built from the PDFs): arnulf-etal-2014, arnulf-etal-2018, arnulf-etal-2021, gefen-larsen-2017, larsen-bong-2016, rosenbusch-etal-2020, nimon-etal-2016. Cited in introduction_prediction (new opening paragraph), introduction_taxonomy, discussion_encodings, discussion_limitations. All seven are FIRST-ROUND candidates for the next reference-validation pass (never validated). Drafting reads: 2014/2018/2021/Rosenbusch read in full or near-full; Gefen-Larsen, Larsen-Bong, Nimon read at abstract + results + discussion/conclusion depth — the per-reference Opus validators must do the complete full-text pass.

## Package-method faithfulness audit — 2026-07-02 (separate from manuscript reference validation)

12 Opus auditors, one per method–source pairing (papers_txt ↔ R/): 0 misimplementations. TEFI numerically identical to EGAnet::tefi (5+ dp); ARI matches Hubert & Arabie Eq. 5 term-for-term; UVA wTO n-independence verified empirically. All attribution-precision fixes applied to the package docs (see NEWS 0.1.1).

## Open items

- OSF link placeholder now in `open_practices.tex` (moved from Method; author to mint the OSF project — link must be LIVE at submission, BRM rejects "upon request"); the OSF archive must include `make_ci_assets.R` + `ci_jackknife.csv` alongside reproduce.R
- CRAN alignment: prose + bib now cite semanticfa 0.1.2; CRAN must host ≥0.1.2 before submission (0.1.1 awaiting email confirmation; 0.1.2 not submitted)
- golino-2026 best-source residue: RESOLVED 2026-07-10 — golino-etal-2021 (primary TEFI paper, MBR 56(6):874-902; PDF read in full, bib built from PDF, all 10 authors, in \shortcites) now cited at both methods_pipeline TEFI-as-fit-index sites ("minimum TEFI", "partition-based TEFI"); golino-2026 kept at its own embedding-dimselect/NMI sites. The 2024 GenTEFI paper (Golino et al., bifactor-vs-correlated comparison) was fetched and read but NOT cited — the manuscript does no bifactor modeling, so it would be a lineage-stretch (papers/Golino_Etal_2024.pdf staged but uncited by design)
- Reference-validation round 3 (confirming round) to declare convergence; statistics + missing-citation passes still unrun
- Semantic-theory-of-survey-response literature paragraph: PDFs to be downloaded by author, then cite in `introduction_prediction.tex`/`introduction_pipelines.tex` and soften the toolkit-gap claim accordingly
- Reference-validation rounds 2–3, then missing-citation / statistics / claim passes
- Em-dash/semicolon policy decision (see pre-flight note)
